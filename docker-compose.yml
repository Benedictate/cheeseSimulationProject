services:
  react-app:
    build: ./Frontend
    ports:
      - "5000:3000"

  node-backend:
    build: ./Backend
    ports:
      - "3001:3001"
    environment:
#      - CORS_ALLOWED_ORIGINS=http://react-app:5000 # only allows frontend inputs, needs extra config for multiple origins
      - MQTT_BROKER_URL=mqtt://mqtt-broker:1883
    depends_on:
      - mqtt-broker

  mqtt-broker:
    image: eclipse-mosquitto:2.0
    ports:
      - "1883:1883"
      - "9001:9001" # optional WebSocket support
    volumes:
      - ./mosquitto/config:/mosquitto/config
      - ./mosquitto/data:/mosquitto/data
      - ./mosquitto/log:/mosquitto/log
